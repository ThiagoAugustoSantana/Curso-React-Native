// Como esse arquivo não usara JSX não sera necessario fazer os imports padrões.

export default props => {
    if(props.teste) {
        return props.children
    } else {
        return false
    }
}














